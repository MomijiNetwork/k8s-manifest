apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-terminal-velocity
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ttyd-velocity
  template:
    metadata:
      labels:
        app: ttyd-velocity
    spec:
      containers:
        - name: ttyd
          image: itzg/minecraft-server:java25
          tty: true
          stdin: true
          args:
            - |
              apt-get update && apt-get install -y ttyd
              ttyd -p 7681 -W rcon-cli
          env:
            - name: EULA
              value: "TRUE"
            - name: RCON_HOST
              value: "minecraft-proxy"
            - name: RCON_PORT
              value: "25575"
            - name: RCON_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rcon-auth
                  key: password
          ports:
            - containerPort: 7681
---
apiVersion: v1
kind: Service
metadata:
  name: ttyd-velocity-service
spec:
  type: NodePort
  ports:
    - port: 7681
      targetPort: 7681
      nodePort: 30780
  selector:
    app: ttyd-velocity
